import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    alignItems: 'center',
    borderRadius: 50,
    padding: 10
  },
  buttonText: {
    fontWeight: 'bold'
  }
});

export default styles;
